import { Box } from "@chakra-ui/react";
import { useState } from "react";

function App() {
  const [auth, setAuth] = useState(false);

  return (
    <>
      <Box mb={20}></Box>
    </>
  );
}

export default App;
